{
  "name": "@mighty-cringe/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/worker.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "node --test dist/*.test.js"
  },
  "dependencies": {
    "@mighty-cringe/contracts": "workspace:*",
    "@mighty-cringe/push": "workspace:*",
    "@mighty-cringe/voice": "workspace:*",
    "dotenv": "^16.6.0",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "tsx": "^4.20.0"
  }
}
