{
  "name": "@mighty-cringe/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "node --test dist/*.test.js"
  },
  "dependencies": {
    "@fastify/cookie": "^11.1.2",
    "@fastify/cors": "^11.0.0",
    "@mighty-cringe/contracts": "workspace:*",
    "@mighty-cringe/db": "workspace:*",
    "@mighty-cringe/voice": "workspace:*",
    "dotenv": "^16.6.0",
    "fastify": "^5.5.0",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "tsx": "^4.20.0"
  }
}
